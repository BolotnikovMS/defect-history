<button
  type="{{ type ?? 'button' }}"
  class="btn btn-color_green btn-color_green-save {{ className ?? '' }}"
>
  @!icons.save({
    optionalClasses: 'icon-size_20'
  })
  {{{ await $slots.main() }}}
</button>
